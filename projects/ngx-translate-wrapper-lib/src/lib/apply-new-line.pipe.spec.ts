import { ApplyNewLinePipe } from './apply-new-line.pipe';

describe('ApplyNewLinePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyNewLinePipe();
    expect(pipe).toBeTruthy();
  });
});
